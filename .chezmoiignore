# Manage .bashrc only on Termux/Android for now. Other Linux hosts keep
# their host-specific .bashrc until branches are added to private_dot_bashrc.tmpl.
{{ if ne .chezmoi.os "android" }}
.bashrc
{{ end }}

# SwiftBar is macOS-only
{{ if ne .chezmoi.os "darwin" }}
.config/swiftbar/**
{{ end }}

# Ignore the age private key backup (if any)
key.txt.age

# Ignore documentation
README.md

# Ignore internal scripts and secret source files
scripts/
secrets/
