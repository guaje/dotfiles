import { notifyPiWaitingForUser } from "../07-native-notify.ts";
export type Ui = { confirm: (title: string, message: string, opts?: { signal?: AbortSignal }) => Promise<boolean>; getEditorText?: () => string; setEditorText?: (text: string) => void; setWorkingVisible?: (visible: boolean) => void; setWorkingMessage?: (message?: string) => void; setWorkingIndicator?: (options?: { frames?: string[]; intervalMs?: number }) => void };
export function writePreview(content: string | undefined) { if (content === undefined) return ""; if (!content) return "<empty file>"; const lines = content.split("\n"), width = String(lines.length).length; return lines.map((line, index) => `${String(index + 1).padStart(width)} │ ${line}`).join("\n"); }
export function editPreview(edits: Array<{ oldText: string; newText: string }> | undefined) { if (!edits?.length) return "<no diff available>"; return edits.flatMap((edit, index) => { const oldLines = edit.oldText ? edit.oldText.split("\n") : ["<empty>"], newLines = edit.newText ? edit.newText.split("\n") : ["<empty>"]; return [`${index ? "\n" : ""}@@ edit ${index + 1} @@`, ...oldLines.map((line, i) => `- ${i + 1} │ ${line}`), ...newLines.map((line, i) => `+ ${i + 1} │ ${line}`)]; }).join("\n"); }
async function hiddenConfirm(ui: Ui, title: string, message: string) { ui.setWorkingVisible?.(false); ui.setWorkingMessage?.(""); ui.setWorkingIndicator?.({ frames: [] }); await new Promise((resolve) => setTimeout(resolve, 0)); try { return await ui.confirm(title, message); } finally { ui.setWorkingMessage?.(); ui.setWorkingIndicator?.(); ui.setWorkingVisible?.(true); } }
type Notify = (body: string, ctx: unknown) => Promise<unknown> | unknown;
export async function confirmFileMutation(ctx: { ui: Ui }, options: { title: string; path: string; preview: string; summary: string }, notify: Notify = notifyPiWaitingForUser) { const previous = ctx.ui.getEditorText?.() ?? ""; await notify(`Approval needed: ${options.title.replace(/\?$/, "")}`, ctx); ctx.ui.setEditorText?.(options.preview); try { return await hiddenConfirm(ctx.ui, options.title, `Path:\n\n${options.path}${options.summary}`); } finally { ctx.ui.setEditorText?.(previous); } }
export async function confirmBash(ctx: { ui: Ui }, message: string, notify: Notify = notifyPiWaitingForUser) { await notify("Approval needed: bash command", ctx); return hiddenConfirm(ctx.ui, "Allow bash command?", message); }
