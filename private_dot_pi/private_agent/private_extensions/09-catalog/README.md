# Dynamic model catalog

`09-catalog` keeps `agent/models.json` provider-only. Its awaited extension factory restores validated owner-only catalog state, refreshes stale custom-provider inventories, and registers available chat models before Pi resolves model scope.

## Commands

- `/catalog` — show inventory, scope, pricing, freshness, missing registrations, and unresolved active prices.
- `/catalog refresh` — refresh custom providers while retaining enabled omissions through temporary discovery failures, then show the unified overview.
- `/catalog sync` — reconcile settings, resolve prices, and interactively review complete Artificial Analysis variant sets for enabled custom and native `/login` models.

`/catalog sync` is interactive because AA UUIDs and thinking levels require review. Discovery and publication call the TypeScript AA service in-process and receive `ctx.signal`; no shell process or temporary batch file is involved. Publication resolves runtime aliases under the writer lock and writes snapshots under the canonical identity, so legacy runtime-identity snapshots remain manageable. The separately reviewed canonical mapping file is never rewritten implicitly. Every shared AA write validates the complete resulting batch and the final canonical pins. A mapping for a model that is no longer enabled may survive a transaction only when its provider, model, thinking level (including generic `null`), and AA UUID exactly match the existing v4 manifest or migrated v3 baseline; new or modified disabled mappings are rejected.

Runtime state and reports allow-list model metadata and never persist API keys, headers, OAuth data, or resolved provider authentication. Provider-supplied `reasoning` and explicit `thinkingLevelMap`/`thinking_level_map` values are validated, persisted, and registered verbatim; an explicit provider map has provenance and wins over both LiteLLM effort metadata and an AA-reviewed fallback. In any supported metadata root, including `model_info` and `litellm_model_info`, LiteLLM may provide `supports_reasoning` plus an explicit Pi `thinking_level_map`, `supported_reasoning_efforts` (or camelCase), or individual `supports_<level>_reasoning_effort` booleans. Precedence is explicit map, complete effort list, then per-level flags. A reasoning-effort list must be non-empty, unique, and contain only `none`, `off`, `minimal`, `low`, `medium`, `high`, `xhigh`, or `max`; malformed lists and flags are ignored. Explicit maps and valid complete effort lists derive provider-provenance Pi maps. Individual LiteLLM flags derive only lower-priority `provider-inferred` evidence because absent flags do not prove a level is unsupported: they conservatively expose advertised values, but a complete AA-reviewed map is retained over them. Explicit provider maps or lists still win over reviewed state. Explicit `none`/`off` supplies the exact off wire value, otherwise off is `null`; gaps clamp upward and then to the highest positively advertised effort, including Pi `xhigh` and `max`.

A custom provider that explicitly supports effort selection can be reviewed at specific levels even when its inventory omitted `reasoning`. Variable-effort models offer only the exact benchmark levels `off`, `low`, `medium`, `high`, `xhigh`, and `max`; fixed-effort models offer only a generic mapping. An existing generic AA mapping is preserved during interactive sync; converting it to specific variants requires a separately reviewed `canonical-mappings.json` change before candidate review. After a complete thinking-specific review, the catalog persists and registers `reasoning: true`; when `off` was reviewed alongside reasoning levels it persists as `off: null`, because an AA off snapshot does not establish a provider wire value. `minimal` maps to the lowest reviewed reasoning level, exact reviewed levels stay exact, and unreviewed higher requests clamp to the next reviewed (or highest) level. Choosing Cancel review aborts that model's review, and incomplete reviews never publish.

`/catalog sync`, CLI discovery, and publication use the authenticated AA **free** endpoint (`/api/v2/language/models/free`). Sync fetches it once, reuses that exact catalog for every confirmed reviewed publication, and then scrapes each selected canonical public page to supplement only null API metrics. Suggestions (including separator-normalized base, non-reasoning, low, medium, and xhigh variants) never alter mappings. Pricing resolves consistently for every provider: provider/catalog/runtime, exact OpenRouter then a unique normalized suffix, explicit numeric Ollama Cloud rate, then reviewed override. Ollama Cloud is loaded lazily once only after a model remains unresolved; availability without a numeric rate neither implies free nor suppresses another source, and is `price-unpublished` only if no rate remains.

Only after the whole `/catalog sync` succeeds, including final catalog reconciliation, does a decisive reviewed-AA cleanup run—and only when at least one successful publication reports `changed: true`. A successful semantic no-op skips that cleanup. It is lock-serialized and removes secure generated snapshots not referenced by the final manifest. Interactive publications defer the normal writer prune so an outer sync failure cannot remove an earlier generation. Direct AA CLI add/replace/refresh paths retain their existing grace-period per-write prune. Cleanup never invokes chezmoi. When an AA generation changed, the final notification lists deterministic net artifact paths relative to the configured snapshot root: `M` for changed `manifest.json` or `canonical-mappings.json` content, `A models/<snapshot>` for final manifest additions, and `D models/<snapshot>` for generated files removed by final cleanup. That net report assumes no concurrent external AA writer during interactive review. It also prints cleanup/capture/publication warnings and suggests `chezmoi status` for review only when changes were captured; it does not run it.

Readers tolerate the publication/cleanup handoff with one bounded retry only if a referenced snapshot disappears and a re-read proves that the manifest content switched. Missing snapshots with an unchanged manifest, corruption, and unsafe permissions still fail closed.

Verified native `compat.supportsReasoningEffort` is retained so fixed-effort models can only receive generic AA mappings. A validated, non-empty provider `thinkingLevelMap` is positive native capability evidence when that boolean is absent; an explicit `false` always wins, and otherwise unknown native effort capability is not treated as selectable. Sync resolves the AA snapshot root and owner-only canonical mappings through the shared AA configuration, including `PI_AA_SNAPSHOT_ROOT`. AA catalog authentication is isolated from separately constructed unauthenticated public-page requests. See [`aa/README.md`](./aa/README.md) for endpoint, streaming, schema, and transaction guarantees.
